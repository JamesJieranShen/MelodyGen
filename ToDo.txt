Note:
- Implement mutate, mutate_note, mutate_length, mutate_length_mod, mutate_prob

Phrase:
- Implement mutate (with 'type' input - all, note, length, etc.)
- Implement generate phrase (based on algorithm)
- Implement different algorithms? 

Build:
- Add AWS S3 script
